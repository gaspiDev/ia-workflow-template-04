# STORY-008: Install Markdown Dependencies

## 1. Description
Install the necessary libraries for parsing and rendering Markdown in the React frontend.

## 2. Tasks
- [x] Install `react-markdown` in the `frontend` directory.
- [x] Install `remark-gfm` in the `frontend` directory.
- [x] Verify `package.json` updates.

## 3. Acceptance Criteria
- Dependencies are correctly added to `frontend/package.json`.
- The project builds successfully after installation.
